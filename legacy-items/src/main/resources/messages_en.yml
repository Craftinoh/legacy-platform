prefix: "&8[&6LegacyItems&8] "

reload-success:
  enabled: true
  text: "{prefix}&aConfiguration reloaded."

reload-failed:
  enabled: true
  text: "{prefix}&cCould not reload configuration."

item-given:
  enabled: true
  text: "{prefix}&aItem &f{item}&a given to &f{player}&a."

item-removed:
  enabled: true
  text: "{prefix}&aItem &f{item}&a removed from &f{player}&a."

item-not-found:
  enabled: true
  text: "{prefix}&cItem not found."

player-not-found:
  enabled: true
  text: "{prefix}&cPlayer not found."

cooldown:
  enabled: true
  text: "&cWait &f{seconds}s &cbefore using this item again."

menu-unavailable:
  enabled: true
  text: "&cThe menu is not currently available."

